{
  "name": "server",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsdown",
    "check-types": "tsc -b",
    "compile": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile server",
    "dev": "bun run --hot src/index.ts",
    "start": "bun run dist/index.mjs"
  },
  "dependencies": {
    "@google/genai": "^2.15.0",
    "@prisma/client": "^7.9.1",
    "@safe-her/auth": "workspace:*",
    "@safe-her/db": "workspace:*",
    "@safe-her/env": "workspace:*",
    "better-auth": "catalog:",
    "dotenv": "catalog:",
    "hono": "^4.12.32",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@safe-her/config": "workspace:*",
    "@types/bun": "catalog:",
    "prisma": "^7.9.1",
    "tsdown": "^0.22.14",
    "typescript": "catalog:"
  }
}
